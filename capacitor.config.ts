import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-eventos-universitarios',
  webDir: 'www'
};

export default config;
